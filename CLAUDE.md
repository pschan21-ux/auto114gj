# auto114gj.com — (주)오토일일사광주 홈페이지

광주 중고차 매매상사 홈페이지. 순수 정적 사이트(HTML/CSS/JS, 빌드 없음).
호스팅: GitHub Pages · 도메인: auto114gj.com (넷리파이 DNS)

## 파일 구조
- `index.html` — 메인 (블랙&화이트 모노크롬 에디토리얼 디자인, 2026-07 확정)
- `car_XX.html` — 차량 상세페이지 (공용 템플릿, 현재 42개)
- `i18n.js` — 다국어 시스템 (한국어→영어·러시아어·중국어·베트남어·아랍어)
- `yt.js` — 유튜브 채널(@auto-114) 최신 영상 모달 (index 전용)
- `images/` — 차량 사진 (img_해시.jpg)
- `차량목록_전체정리.txt` — 차량 목록 참고 자료

## 필수 규칙
1. **모든 새 차량 페이지는 `</body>` 앞에 `<script src="i18n.js" defer></script>` 포함할 것.**
2. **새 한국어 문구를 추가하면 `i18n.js`의 사전(D)에 5개 언어 번역을 함께 추가할 것.**
   (템플릿 공용 문구는 이미 등록됨. 차명은 WORD 로마자 맵이 자동 처리.)
3. 메인 디자인은 블랙&화이트 모노크롬 유지 — 블루·라임 등 컬러 포인트 제안 금지.
   단, 히어로 배경 사진(`.hphoto` / `images/hero_office.jpg`)은 사장님 지시로 **원본 컬러 유지**(2026-08-26 확정) — 흑백으로 되돌리지 말 것.
   차량 카드 배지 인라인: `background:#111417;color:#fff;` / 버튼은 `.cbtn` (인라인 없음).
4. 검색 `<select>`의 option은 value 속성이 한국어 원문으로 고정됨(i18n이 자동 처리) — 검색 로직은 한국어 값 기준.
5. 백업 파일(`*_bak*`, `_price_backup_*`)은 커밋하지 않음 (.gitignore 처리됨).

## 배포 (자동)
- `main` 브랜치에 push → GitHub Pages가 1~2분 내 자동 반영
- 수동 조작 불필요. push가 곧 배포다.

## 운용 규칙 (사장님과의 대화 — 2026-07-13 확정)
- **사장님은 그냥 수정 요청만 말한다.** 별도 시작/종료 명령 없음.
- 수정 요청을 받으면: 수정 → 검증(미리보기 또는 파일 확인) → **자동으로 커밋 + `git push`(=배포)** → "2~3분 후 auto114gj.com에 반영됩니다" 보고. 사장님에게 푸시 여부를 따로 묻지 않는다.
- 단, 디자인이 크게 바뀌는 작업은 푸시 전에 미리보기로 먼저 보여드리고 확인받는다.
- ("출근"/"퇴근"이라는 말을 쓰시면 각각 = 준비(pull+미리보기 서버) / 일괄 마무리(검증+커밋+푸시)로 처리하되, 필수 아님.)
- 차량 추가/삭제 워크플로는 로컬 메모리 `auto114-car-add-workflow` 참고 (엔카 API 추출 → 상세페이지 생성 → index 카드 추가 → sitemap.xml 갱신).
- git 사용자 설정: `-c user.name="auto114gj" -c user.email="pschan21@gmail.com"` / 원격: https://github.com/pschan21-ux/auto114gj (gh CLI 인증됨, gh 경로: C:\Program Files\GitHub CLI\gh.exe)

## 미리보기
- `.claude/launch.json`(상위 작업 폴더)의 `auto114-site` — PowerShell 간이 서버, 포트 8890.
- PS1 스크립트는 반드시 UTF-8 BOM으로 저장.

## 커밋 규칙
- 커밋 메시지는 한국어로, 무엇이 바뀌었는지 사장님이 읽어도 이해되게.
- 차량 추가: `차량 추가: [차명] (car_XX)` / 판매완료: `판매완료 처리: [차명]`
